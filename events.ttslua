function onPlayerChangeColor(player_color)
    if Game.state ~= "JOIN" then
        if getDisconnect() then
            Game.paused = true
        else
            Game.paused = false
        end
    end
end
